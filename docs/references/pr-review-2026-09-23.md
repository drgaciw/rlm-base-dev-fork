# Dependabot PR Review — 2026-09-23

| | |
|---|---|
| Repo | [drgaciw/rlm-base-dev-fork](https://github.com/drgaciw/rlm-base-dev-fork) |
| Base | `main` @ `6fda0364` (squash of the wave 1–3 overhaul, PR #1) |
| PRs reviewed | #2–#12: all 11 open Dependabot PRs, all opened after WP-09 enabled `.github/dependabot.yml` |
| Reviewer | ARCHITECT, read-only (the only file written is this one) |
| Summary | **9 MERGE · 1 FIX-THEN-MERGE · 0 HOLD · 1 CLOSE** |

## Method

- **Diffs.** Read with `gh pr diff` for every PR. Each one is a one-line or few-line version or floor change, produced entirely by Dependabot.
- **CI.** Read with `gh pr checks` on each PR's current head, all based on `6fda0364`. For the two failing runs I also read the logs with `gh run view --log-failed`.
- **SHA pins.** Every SHA-pinned action bump was checked against its tag with `gh api repos/<action>/commits/<tag>`.
- **Breaking changes.** Every major-version bump's release notes were read (`gh api .../releases/tags/<tag>`) and compared with the inputs our workflows actually use.
- **pip resolution.** The robot pip bumps were resolved with `uv pip compile --python-version 3.11` and `3.13`, both before and after, alone and together with `cumulusci==4.8.1`.
- **npm resolution.** The eslint 10 peer chain was checked with `npm view`.

### Correction to the known CI facts

The briefing said "Lint (changed files) fails on every PR." That is **not** what the current checks show. As of the runs at 2026-09-23T13:50Z, **only #8 and #12** fail. PRs #2–#7 and #9–#11 are fully green: Lint, Mechanical checks and the Docker ARG-defaults check all pass. `prepare-rlm-org` and `claude-review` are skipped by design.

- **#8 fails Prettier on `.github/workflows/agent-tooling-optimization.yml`.** The changed-files selection is working as designed. #8 is the only PR that *touches* that file, and Prettier checks whole files, so the file's existing formatting debt surfaces there.
- **#3 also touches workflows** (`pr-checks.yml`, `prepare-rlm-org.yml`), and Prettier passes on it.
- **#12 fails `npm install` with ERESOLVE.** See the table below.

## Verdicts

| PR | Title | Risk | Major? | Verdict | Required changes (exact) | Group |
|---|---|---|---|---|---|---|
| **#2** | bump docker/setup-qemu-action 3.7.0 → 4.4.0 | M | y | **MERGE** | None to the PR. The SHA `99012661…204e1` matches tag `v4.4.0` and the `# v4.4.0` comment is correct.<br>v4.0.0 only changes the runtime: it defaults to Node 24 and needs runner ≥ 2.327.1, which hosted `ubuntu-latest` satisfies. We pass no inputs.<br>**After merge:** run the G1 smoke dispatch. | G1 |
| **#3** | bump actions/setup-node 5 → 7 | L | y | **MERGE** | None. The v6 breaking change (automatic caching limited to npm) doesn't apply: neither `pr-checks.yml` nor `prepare-rlm-org.yml` sets `cache:`, and both pin versions via `node-version-file` / `node-version`.<br>v7 changes are internal (ESM migration, removal of the dummy `NODE_AUTH_TOKEN` export, which we don't use).<br>The Lint job on this PR already ran on setup-node@v7 and passed. `prepare-rlm-org` (label-gated) is not exercised by the PR; the next `ci:prepare-org` label run covers it.<br>This is first-party `actions/*`, tag-pinned, consistent with WP-09 (only write-scoped third-party actions are SHA-pinned). | — |
| **#4** | bump docker/setup-buildx-action 3.12.0 → 4.4.1 | M | y | **MERGE** | None to the PR. The SHA `f87e5991…97069` matches tag `v4.4.1`.<br>v4.0.0 defaults to Node 24 and **removes deprecated inputs/outputs**. `docker-publish.yml:115` passes **no** inputs, so nothing is affected.<br>**After merge:** run the G1 smoke dispatch. | G1 |
| **#5** | robot: `selenium` `>=4.10,<5` → `>=4.49.0,<5` | L | n | **MERGE** | None. **This raises the floor only:** today's resolution is already selenium 4.49.0 on Python 3.11 and 3.13, so a fresh install is byte-for-byte unchanged. | G2 |
| **#6** | robot: `robotframework-seleniumlibrary` `>=6,<7` → `>=6.9.0,<7` | L | n | **MERGE** | None. Floor only; resolves to 6.9.0 both before and after. | G2 |
| **#7** | bump docker/build-push-action 6.19.2 → 7.4.0 | M | y | **MERGE** | None to the PR. The SHA `c3c9e263…d91b0dc` matches tag `v7.4.0`.<br>v7.0.0 moves to Node 24, removes the `DOCKER_BUILD_NO_SUMMARY` and `DOCKER_BUILD_EXPORT_RETENTION_DAYS` env vars, and removes legacy export-build summary support. None of that is used in `docker-publish.yml`, whose inputs `context`, `file`, `platforms`, `push`, `build-args`, `tags` and `labels` are unchanged in v7.<br>**After merge:** run the G1 smoke dispatch. | G1 |
| **#8** | bump actions/checkout 6 → 7 (6 workflows, 10 call sites) | L | y | **FIX-THEN-MERGE** | **No change to the PR itself.** It is blocked only by the base-branch Prettier debt in `agent-tooling-optimization.yml`.<br>1. Wait for **PR-00** to land on `main`.<br>2. Comment `@dependabot rebase` on #8.<br>3. Merge once "Lint (changed files)" is green.<br>v7.0.0 notes list only dependency bumps and release prep, with no input changes. Every call site keeps its existing `with:` block, including `persist-credentials: false` and the pinned `ref:` for the vendored `anthropics/claude-code` marketplace. | — |
| **#9** | robot: `robotframework` `>=6,<8` → `>=7.5,<8` | L | n | **MERGE** | None. Floor only; resolves to 7.5 both before and after. The raised floor drops Robot Framework 6.x support, which was never installed fresh, since pip already picks 7.x. | G2 |
| **#10** | robot: `webdriver-manager` `>=4,<5` → `>=4.1.2,<5` | L | n | **MERGE** | None. Floor only; resolves to 4.1.2 both before and after. | G2 |
| **#11** | robot: `urllib3` `>=2.6.3` → `>=2.8.0` | L | n | **MERGE** | None. Floor only; resolves to 2.8.0 both before and after, including when resolved together with `cumulusci==4.8.1` (which resolves urllib3 2.8.0 on its own). | G2 |
| **#12** | bump eslint 9.39.5 → 10.11.0 (dev) | H | y | **CLOSE** | Comment **`@dependabot ignore this major version`** on #12, then close it.<br>The whole LWC lint chain is still `eslint ^9`-only: `@salesforce/eslint-config-lwc@4.1.2` (latest, modified 2026-04-17) peers `eslint: ^9` and `@lwc/eslint-plugin-lwc ^3`; `@lwc/eslint-plugin-lwc@3.5.0` (latest) peers `eslint: ^9`; `@babel/eslint-parser@7.29.9` peers `eslint ^7.5 \|\| ^8 \|\| ^9`. So `npm install` fails with ERESOLVE.<br>Do **not** use `--legacy-peer-deps`.<br>Revisit when `@salesforce/eslint-config-lwc` publishes an eslint 10 peer range (follow-up F4). | — |

## Worker grouping

| Group | PRs | Owner | Procedure |
|---|---|---|---|
| **G1: docker/* actions** | #2, #4, #7 | one worker | 1. Merge all three. They edit different lines of `docker-publish.yml` (112, 115, 125), so they should not conflict; if one does, comment `@dependabot rebase`.<br>2. **Before the Monday 07:00 UTC scheduled publish**, trigger `docker-publish.yml` via `workflow_dispatch` with `tag: ci-smoke-2026-09-23` and `platforms: linux/amd64`. **Never** use tag `latest` for this.<br>3. Confirm the three steps run on their new majors and the image pushes, then delete the `ci-smoke-*` package version from GHCR.<br>No PR check exercises this workflow, which is why G1 is rated M despite a clean diff. |
| **G2: robot pip floors** | #5, #6, #9, #10, #11 | one worker | 1. Merge **sequentially**. All five edit adjacent lines of the same 9-line `robot/requirements.txt`, so each merge conflicts the rest.<br>2. After each merge, Dependabot auto-rebases the others. If a PR shows conflicts and doesn't rebase within a few minutes, comment `@dependabot rebase`.<br>3. Final content must equal `robotframework>=7.5,<8`, `robotframework-seleniumlibrary>=6.9.0,<7`, `selenium>=4.49.0,<5`, `webdriver-manager>=4.1.2,<5`, `urllib3>=2.8.0`. |
| **Standalone** | #3 | any worker | Merge. |
| **After PR-00** | #8 | any worker | `@dependabot rebase`, then merge when green. |
| **Close** | #12 | any worker | `@dependabot ignore this major version`, then close. |

**Suggested order:** #3 → G1 (#2, #4, #7) → G2 (#5, #6, #9, #10, #11) → PR-00 → #8 → close #12. G1, G2 and #3 are independent and can run in parallel. The only ordering constraint is PR-00 before #8.

## PR-00: base-branch lint fix (prerequisite for #8)

**Status: in progress, worker-01, branch `fix/lint-changed-files` on the fork.**

| Field | Value |
|---|---|
| Blocks | #8 only. The other PRs are already green; see the correction above. |
| Root cause | `.github/workflows/agent-tooling-optimization.yml` on `main` is not Prettier-formatted. The Lint job's changed-files selection is correct, but Prettier checks whole files, so any PR that touches this file inherits its existing debt. |
| Owned files | `.github/workflows/agent-tooling-optimization.yml`. Also `.github/workflows/pr-checks.yml`, only if worker-01 changes the Prettier selection. |
| Acceptance | 1. `npx prettier --check .github/workflows/*.yml` exits 0 on the PR-00 branch, so no other workflow carries the same latent debt.<br>2. The reformat is formatting-only: `git diff -w` shows no semantic change, and `actionlint` reports 0 findings.<br>3. PR-00's own "Lint (changed files)" and "Mechanical checks" are green.<br>4. After PR-00 merges and #8 is rebased, #8's Lint job is green. |

## Follow-ups (no PR yet)

| # | Finding | Evidence | Recommendation |
|---|---|---|---|
| F1 | **Dependabot fan-out.** Eleven single-dependency PRs, five of them on the same 9-line file, guarantee rebase churn. | `.github/dependabot.yml` has no `groups:` | Add `groups:`: one `docker-actions` group (`docker/*`), one `robot-pip` group (the `/robot` pip ecosystem, minor + patch), and one `github-actions` group for minor + patch. Majors stay individual, so each gets its own review. |
| F2 | **CumulusCI and robot requirements conflict (this was already the case on `main`).** `cumulusci==4.8.1` declares `selenium<4` and resolves `robotframework-seleniumlibrary` 5.1.3 / selenium 3.141.0. `robot/requirements.txt` requires `selenium>=4.10` and `seleniumlibrary>=6`. Resolving both together is **unsatisfiable** (`uv pip compile`). CI works only because `prepare-rlm-org.yml:130-132` installs CumulusCI and then the robot requirements in **separate** `pip install` calls, and pip overrides CumulusCI's pin with a warning. The Dockerfile's pipx inject and `requirements-dev.txt` follow the same pattern. These PRs neither cause nor worsen it. | `uv pip compile base.txt robot/requirements.txt` → "cumulusci==4.8.1 depends on selenium<4 … unsatisfiable" | Record the intentional override: a short comment at `prepare-rlm-org.yml:132` and in `robot/requirements.txt`, and in `requirements-dev.txt` if it merges them, saying the robot stack deliberately overrides CCI 4.8.1's selenium pin. Otherwise a future "fix the resolver warning" change will downgrade selenium to 3.x and break the Robot suites. |
| F3 | **The vendored-marketplace checkout step is tag-pinned** (`actions/checkout@v6`, `@v7` after #8), not SHA-pinned as WP-13 AC2 specified. The marketplace content itself *is* SHA-pinned via `ref:`. | `.github/workflows/claude-code-review.yml` vendored-checkout step | Low priority: it's a first-party action with read-only `contents`. SHA-pin it if the repo adopts a pin-everything policy. |
| F4 | **eslint 10 is blocked upstream.** | `npm view @salesforce/eslint-config-lwc peerDependencies` → `eslint: ^9` | Recheck `@salesforce/eslint-config-lwc` and `@lwc/eslint-plugin-lwc` monthly. Once both accept eslint 10, remove the ignore and let Dependabot reopen the bump. |

## Addendum: #13, #14 and maintainer actions (2026-09-23, later the same day)

Everything below was checked live with `gh pr view`, `gh pr checks`, `gh api .../compare` and `gh api .../commits`. `main` is now at `3aaee27c` (the merge of #14).

### New PRs

| PR | Title | Risk | Major? | Verdict | Required changes (exact) | Group |
|---|---|---|---|---|---|---|
| **#14** | fix(ci): reformat agent-tooling-optimization.yml for prettier (PR-00, worker-01) | L | n | **MERGE**. **Already merged** as `3aaee27c`. | None.<br>The diff is one whitespace-only line: a double space before `# v8.1.1` on the `peter-evans/create-pull-request` SHA-pinned line in `agent-tooling-optimization.yml` becomes a single space. The SHA and the version comment are unchanged.<br>All checks were green: Lint (changed files), Mechanical checks, Baseline static checks, Docker ARG defaults.<br>This meets PR-00 acceptance 1–3. Acceptance 4 (#8 green after rebase) can't be tested until #8 exists again; see the maintainer-action table below. | PR-00 |
| **#13** | fix: CI gate suite fixes and prettier formatting follow-up (`agent-review-2026-09` → `main`) | L | n | **CLOSE** | Close it without merging. It's a no-op:<br>• `gh api repos/drgaciw/rlm-base-dev-fork/compare/main...agent-review-2026-09` gives `ahead_by=7`, `behind_by=0`, **`files=0`**, and `gh pr diff 13` is 0 bytes. The branch tree is already byte-identical to `main`.<br>• Its seven commits (`0c2af73d` overhaul, `ac4aff11` gate/PLW1514 fixes, four prettier style commits, `929b5a15` merge of fork/main) are content the #1 squash `6fda0364` already carried.<br>• **Merging it** would add a second, unsquashed copy of the overhaul history to `main` for no content change. **Squash-merging it** would produce an empty commit.<br>• Its green checks are therefore not evidence of anything new.<br>Optionally delete the `agent-review-2026-09` branch afterwards. Ask the maintainer first: it was the orchestrator's working branch. | — |

### Maintainer actions vs. this review's verdicts

| PR | Verdict above | Live state | Reconciliation / next step |
|---|---|---|---|
| #2, #3, #4, #5, #6, #9, #10, #11 | MERGE | **Merged** | Done. G2 (robot floors) is complete: `robot/requirements.txt` on `main` now has all five floors. |
| **#7** (docker/build-push-action 6.19.2 → 7.4.0) | MERGE | **Still OPEN** (CLEAN, all checks green). `main` still has `build-push-action@10e90e36… # v6.19.2` at `docker-publish.yml:125`. | **Correction to the orchestrator's summary:** #7 was **not** merged. Merge it; no changes are needed and it rebases cleanly on `3aaee27c`. G1 is incomplete until it lands. `main` currently mixes qemu v4 and buildx v4 with build-push v6. That combination is compatible, but **run the G1 `ci-smoke` workflow_dispatch only after #7 merges**, and before the Monday 07:00 UTC scheduled publish. |
| **#8** (actions/checkout 6 → 7) | FIX-THEN-MERGE after PR-00 | **Closed** 14:15Z. The head branch `dependabot/github_actions/actions/checkout-7` has been **deleted** (404). Dependabot replied "won't notify you again about this release". | Now that #14 has landed, the prerequisite is met, but nothing will re-propose v7.0.0 automatically: the branch is gone, so a plain GitHub "Reopen" won't work, and Dependabot has suppressed this release.<br>1. **Preferred:** comment **`@dependabot reopen`** on #8. That recreates the branch at the current base; then merge once "Lint (changed files)" is green. This is the proof of PR-00 acceptance 4.<br>2. **Fallback, if Dependabot doesn't respond:** a small manual PR that replaces `actions/checkout@v6` with `actions/checkout@v7` at all 10 call sites in the same 6 workflows, touching nothing else. Lint must be green and `actionlint` must report 0 findings. |
| **#12** (eslint 9 → 10) | CLOSE with `@dependabot ignore this major version` | **Closed**, but with only per-release suppression. Dependabot replied "won't notify you again about **this release**, but will get in touch when a new version is available". | Not finished. The next eslint 10.x patch will produce a new PR that fails the same ERESOLVE. Comment **`@dependabot ignore this major version`** on #12, which works on closed PRs, so the whole 10.x line is skipped until F4 clears. |

**Updated tally:**

| Verdict | PRs |
|---|---|
| MERGE (11) | #2, #3, #4, #5, #6, #7, #9, #10, #11, #14; and #8 once recreated |
| CLOSE (2) | #12, #13 |
| Still open | #7 (merge), #13 (close) |
| Still to do | #8 recreate; #12 major-version ignore |
